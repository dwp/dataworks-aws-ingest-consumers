groups:
  - name: coalesce-storage
    jobs:
      - coalesce-storage-development
      - coalesce-storage-qa
      - coalesce-storage-integration
      - coalesce-storage-preprod
      - coalesce-storage-production

  - name: coalesce-manifests
    jobs:
      - coalesce-manifests-development
      - coalesce-manifests-qa
      - coalesce-manifests-integration
      - coalesce-manifests-preprod
      - coalesce-manifests-production

  - name: update-pipeline
    jobs:
      - update-pipeline-corporate-storage-coalescence
